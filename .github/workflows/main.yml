name: Infinity Chat CD

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.10'
        
    - name: Run Syntax & Unit Tests
      run: |
        pip install -r requirements.txt
        python -m py_compile backend/*.py
        
    - name: Build Docker Image
      run: |
        docker build -t zeeshanpy/infinity-chat:latest .
        
    # Placeholder for actual deployment command
    # - name: Deploy to Cloud
    #   run: kubectl apply -f backend/deployment/k8s-deployment.yaml
